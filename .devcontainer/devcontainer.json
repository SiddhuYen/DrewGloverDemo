{
  "name": "ARTEMIS - Warm-Intro Pathfinder",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",

  // One-time: install the app + the spaCy model the NER pipeline needs.
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt && python -m spacy download en_core_web_sm",

  // Every start: boot the app in the background on the forwarded port.
  "postStartCommand": "nohup bash .devcontainer/start.sh > /tmp/artemis.log 2>&1 & echo 'ARTEMIS starting on port 8000'",

  // 8000 auto-forwards; Codespaces pops the app open in a browser tab.
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "ARTEMIS app",
      "onAutoForward": "openBrowser"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
