*.db
*.db-shm
*.db-wal
.env
graphs/
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.DS_Store

# Windows build (PyInstaller). build_assets/ holds the graph that gets bundled;
# it is generated by build_windows.ps1 and covered by *.db above.
build/
dist/
build_assets/

# The staged binary. Netlify uploads it from the publish dir at deploy time;
# committing 72MB would be permanent in git history and in every clone.
docs/*.exe
.netlify/

# Instagram scraper session + output (never commit a login session)
.ig_profile/
data/ig_mutuals.json
data/drew_ig_mutuals.json
.ig_session
data/*.progress.json
.x_session
data/x_mutuals.json
data/x_mutuals.progress.json
